apiVersion: v1
kind: Service
metadata:
  name: {{ include "benias-game.fullname" . }}
  labels:
    {{- include "benias-game.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "benias-game.selectorLabels" . | nindent 4 }}
